---

---

<section class="py-24 bg-gray-50 border-t border-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
                How it works
            </h2>
            <p class="mt-4 text-lg text-gray-600">
                From GitHub to Docs in three simple steps.
            </p>
        </div>

        <div class="grid grid-cols-1 gap-12 md:grid-cols-3">
            {/* Step 1 */}
            <div class="relative text-center">
                <div
                    class="flex items-center justify-center h-16 w-16 rounded-full bg-blue-100 text-blue-600 text-2xl font-bold mx-auto mb-6"
                >
                    1
                </div>
                <h3 class="text-xl font-bold text-gray-900 mb-2">
                    Internalize Your Work
                </h3>
                <p class="text-gray-600">
                    Paste your GitHub repository URL. We support public
                    repositories out of the box.
                </p>
            </div>

            {/* Step 2 */}
            <div class="relative text-center">
                <div
                    class="flex items-center justify-center h-16 w-16 rounded-full bg-blue-100 text-blue-600 text-2xl font-bold mx-auto mb-6"
                >
                    2
                </div>
                <h3 class="text-xl font-bold text-gray-900 mb-2">
                    We Parse & Process
                </h3>
                <p class="text-gray-600">
                    Our engine grabs your README.md, optimizes images, generates
                    navigation, and applies a clean theme.
                </p>
            </div>

            {/* Step 3 */}
            <div class="relative text-center">
                <div
                    class="flex items-center justify-center h-16 w-16 rounded-full bg-blue-100 text-blue-600 text-2xl font-bold mx-auto mb-6"
                >
                    3
                </div>
                <h3 class="text-xl font-bold text-gray-900 mb-2">
                    Instant Documentation
                </h3>
                <p class="text-gray-600">
                    Your documentation site is ready. Fast, searchable, and
                    always in sync with your codebase.
                </p>
            </div>
        </div>
    </div>
</section>
