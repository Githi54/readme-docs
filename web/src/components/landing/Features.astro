---
const features = [
    {
        name: "Zero Configuration",
        description:
            "No config files, no build steps. Just point to your repo and go.",
        icon: "⚡",
    },
    {
        name: "Git-Native",
        description: "Updates automatically when you push to your main branch.",
        icon: "🔄",
    },
    {
        name: "Instant Search",
        description:
            "Full-text search built-in ensuring users find what they need.",
        icon: "🔍",
    },
];
---

<section class="py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
                Everything you need
            </h2>
            <p class="mt-4 text-lg text-gray-600">
                Focus on writing good documentation, we handle the presentation.
            </p>
        </div>

        <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-4">
            {
                features.map((feature) => (
                    <div class="relative p-6 bg-white border border-gray-100 rounded-xl hover:shadow-md transition-shadow">
                        <div class="text-4xl mb-4">{feature.icon}</div>
                        <h3 class="text-lg font-bold text-gray-900 mb-2">
                            {feature.name}
                        </h3>
                        <p class="text-gray-600 text-sm leading-relaxed">
                            {feature.description}
                        </p>
                    </div>
                ))
            }
        </div>
    </div>
</section>
