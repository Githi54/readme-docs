---
import { ROUTES } from "../../consts";
---

<section class="relative pt-24 pb-32 overflow-hidden">
    <div
        class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center"
    >
        <div
            class="inline-flex items-center px-3 py-1 rounded-full border border-indigo-500/30 bg-indigo-500/10 text-indigo-300 text-xs font-medium mb-8 animate-fade-in-up"
        >
            <span
                class="flex h-2 w-2 rounded-full bg-indigo-400 mr-2 animate-pulse"
            ></span>
            v1.0 is now available
        </div>
        <h1
            class="text-4xl tracking-tight font-extrabold text-white sm:text-5xl md:text-6xl mb-6 text-glow"
        >
            <span class="block">Turn your README into a</span>
            <span
                class="block text-indigo-400 bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 to-purple-400"
                >documentation site</span
            >
        </h1>
        <p
            class="mt-4 max-w-2xl mx-auto text-xl text-gray-400 mb-10 leading-relaxed"
        >
            Zero configuration. Blazing fast. Just your GitHub URL.
            <br class="hidden md:block" />Stop fighting with heavy documentation
            tools.
        </p>

        <div class="mt-8 flex flex-col sm:flex-row justify-center gap-4">
            <a
                href={ROUTES.GENERATE}
                class="inline-flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-500 md:py-4 md:text-lg transition-all shadow-[0_0_20px_rgba(99,102,241,0.5)] hover:shadow-[0_0_30px_rgba(99,102,241,0.7)]"
            >
                Generate Docs
            </a>
            <a
                href={ROUTES.GENERATE}
                class="inline-flex items-center justify-center px-8 py-3 border border-gray-700 text-base font-medium rounded-md text-gray-300 bg-white/5 hover:bg-white/10 md:py-4 md:text-lg transition-colors backdrop-blur-sm"
            >
                View Example
            </a>
        </div>
    </div>

    {/* Abstract background decoration */}
    <div
        class="absolute top-0 left-0 right-0 h-full overflow-hidden pointer-events-none"
    >
        <div
            class="absolute top-[-20%] left-[20%] w-[60%] h-[60%] bg-indigo-900/20 rounded-full blur-[120px]"
        >
        </div>
        <div
            class="absolute bottom-[-10%] right-[10%] w-[40%] h-[40%] bg-purple-900/20 rounded-full blur-[100px]"
        >
        </div>
    </div>
</section>
