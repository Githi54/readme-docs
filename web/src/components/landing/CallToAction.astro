---
import { ROUTES } from "../../consts";
---

<section class="relative py-16 overflow-hidden">
    <div class="absolute inset-0 bg-indigo-600/10"></div>
    <div
        class="absolute inset-0 bg-gradient-to-t from-[#0B0C10] to-transparent"
    >
    </div>

    <div
        class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative z-10"
    >
        <h2
            class="text-3xl font-extrabold text-white sm:text-4xl mb-8 text-glow"
        >
            Ready to upgrade your docs?
        </h2>
        <p class="text-xl text-indigo-200 mb-10 max-w-2xl mx-auto">
            No sign up required for public repositories. Free forever for open
            source.
        </p>
        <div class="flex justify-center gap-4">
            <a
                href={ROUTES.GENERATE}
                class="inline-flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-500 md:py-4 md:text-lg transition-all shadow-[0_0_20px_rgba(99,102,241,0.5)] hover:shadow-[0_0_30px_rgba(99,102,241,0.7)]"
            >
                Try it now
            </a>
        </div>
    </div>
</section>
